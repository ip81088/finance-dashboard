apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: finance-dashboard
  namespace: finance
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: finance-dashboard
                port:
                  number: 80
